spring:
  application:
    name: prof.ia
  docker:
    compose:
      enabled: true
      file: compose.yml
  threads:
    virtual:
      enabled: true
  ai:
    ollama:
      base-url: http://localhost:9090
      chat:
        model: llama3:instruct
  data:
    mongodb:
      username: root
      password: password
      authentication-database: admin
      database: profia

vaadin:
  url-mapping: /profia/*
  bun:
    enable: true
